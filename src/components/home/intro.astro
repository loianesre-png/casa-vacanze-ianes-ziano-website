---
// src/components/IntroSection.astro
import { Image } from 'astro:assets';
import { ViewTransitions } from 'astro:transitions';
import heroImage from '../../assets/images/suiteDeluxe/20241130_155610.webp';
import HouseIcon from '../ui/house.astro';

const apartments = [
  { title: 'Bilocale 2', subtitle: 'posti letto', href: '#mini' },
  { title: 'Trilocale 4', subtitle: 'posti letto', href: '#medio' },
  { title: 'Appartamento', subtitle: '8 posti letto', href: '#grande' }
];
---

<ViewTransitions />

<section class="w-full  bg-white ">
  <div class="max-w-full min-h-fit lg:h-[80vh] flex flex-col">
    <div class="flex flex-col lg:flex-row flex-1 overflow-hidden">
      <div class="w-full lg:w-1/2 p-6 min-h-fit lg:p-16 flex flex-col justify-center order-2 lg:order-1">
        <h1 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-4 lg:mb-6">
          Il tuo rifugio a Trento, tra natura e comfort
        </h1>
        <p class="text-base lg:text-lg text-gray-600 leading-relaxed">
          Situata a Villazzano, una tranquilla collina a soli 5 km dal centro di Trento, Casa Negrano offre due
          appartamenti accoglienti e confortevoli, ideali per chi desidera esplorare il Trentino o rilassarsi immerso
          nella natura. Circondata dalla bellezza delle montagne e collegata agevolmente alla città, la nostra struttura
          è perfetta per famiglie, coppie, gruppi di amici e chi viaggia per lavoro.
        </p>
        
        <div class="flex flex-col sm:flex-row justify-start gap-4 lg:gap-12 p-4 lg:p-8 bg-white">
          {apartments.map((apt) => (
            <a href={apt.href} class="flex items-center gap-3 py-2 lg:py-0">
              <div class="bg-green-500 p-2 rounded-full">
                <HouseIcon class="h-5 w-5 text-white" />
              </div>
              <div class="flex flex-col">
                <span class="font-medium text-gray-900">{apt.title}</span>
                <span class="text-gray-600">{apt.subtitle}</span>
              </div>
            </a>
          ))}
        </div>
      </div>

      <div class="w-full lg:w-1/2 overflow-hidden h-[50vh] lg:h-auto order-1 lg:order-2">
        <div class="w-full h-full">
          <Image 
            src={heroImage}
            alt="Casa Negrano - Vista esterna"
            class="w-full h-full object-cover"
            width={heroImage.width}
            height={heroImage.height}
            loading="eager"
            fetchpriority="high"
            format="webp"
            quality={80}
          />
        </div>
      </div>
    </div>
  </div>
</section>